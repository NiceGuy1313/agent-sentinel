from setuptools import setup,find_packages

setup(
   name='magic_image_processor',
   version='1.0',
   description='A useful image module',
   author='Man Foo',
   author_email='foomail@foo.example',
   packages=find_packages(),  #same as name
)